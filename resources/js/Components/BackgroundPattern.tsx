import React from "react";
import LogoText from "@/Components/LogoText";
import {
    NavigationMenu,
    NavigationMenuItem,
    NavigationMenuLink,
    NavigationMenuList,
    navigationMenuTriggerStyle
} from "@/Components/NavigationMenu";
import {Button} from "@/Components/Button";

export default function BackgroundPattern(props: React.SVGProps<SVGSVGElement>) {
    return (

    )
}
